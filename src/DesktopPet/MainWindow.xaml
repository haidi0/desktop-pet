<Window x:Class="DesktopPet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="桌面宠物"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="200" Height="300"
        WindowStartupLocation="Manual">

    <Grid>
        <!-- 右键菜单 -->
        <Grid.ContextMenu>
            <ContextMenu Background="#2D2D30" Foreground="White" BorderBrush="#3F3F46">
                <MenuItem Header="显示宠物" Foreground="White"/>
                <MenuItem Header="切换角色" Foreground="White"/>
                <Separator/>
                <MenuItem Header="设置" Foreground="White"/>
                <Separator/>
                <MenuItem Header="退出" Foreground="White" Click="Exit_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>

        <!-- 宠物图像 -->
        <Image x:Name="PetImage"
               RenderTransformOrigin="0.5,0.5"
               MouseLeftButtonDown="PetImage_MouseLeftButtonDown"
               MouseMove="PetImage_MouseMove"
               MouseLeftButtonUp="PetImage_MouseLeftButtonUp"
               MouseRightButtonDown="PetImage_MouseRightButtonDown">
            <Image.RenderTransform>
                <ScaleTransform x:Name="PetScale" ScaleX="1" ScaleY="1"/>
            </Image.RenderTransform>
        </Image>
    </Grid>
</Window>
